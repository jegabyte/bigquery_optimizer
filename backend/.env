# Google Cloud Configuration
GOOGLE_CLOUD_PROJECT=aiva-e74f3
GOOGLE_CLOUD_LOCATION=us-central1

# Use Application Default Credentials (ADC)
# No need for GOOGLE_APPLICATION_CREDENTIALS when using ADC

# ADK Configuration
GOOGLE_GENAI_USE_VERTEXAI=True  # Use Vertex AI with ADC
# GOOGLE_GENAI_API_KEY not needed when using Vertex AI with ADC

# BigQuery Settings
BIGQUERY_DATASET=analytics  # Using existing analytics dataset
BIGQUERY_LOCATION=US

# ADK Settings
ADK_LOG_LEVEL=INFO
ADK_PORT=8000

# Cloud Trace Configuration
ADK_TRACE_TO_CLOUD=true  # Enable Cloud Trace for monitoring