# ================================================================
# BigQuery Optimizer - Environment Variables
# ================================================================
# Copy this file to .env and update with your values
# These variables are used by deploy.sh and all services
# ================================================================

# Primary Google Cloud Configuration
GCP_PROJECT_ID=your-project-id        # Your Google Cloud Project ID
REGION=us-central1                     # Deployment region

# BigQuery Configuration
BQ_PROJECT_ID=${GCP_PROJECT_ID}       # BigQuery project (defaults to GCP_PROJECT_ID)
BQ_DATASET=bq_optimizer                # BigQuery dataset name
BQ_LOCATION=US                         # BigQuery location (US, EU, etc.)

# Application Environment
APP_ENV=development                    # Environment: development, staging, production
DEBUG=false                            # Enable debug logging

# Service Ports (for local development)
AGENT_API_PORT=8000                    # Agent API port
BACKEND_API_PORT=8001                  # Backend API port  
FRONTEND_PORT=3000                     # Frontend port

# CORS Configuration
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Optional: Google Cloud Credentials
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# Optional: Vertex AI Configuration (for ADK)
# VERTEX_AI_LOCATION=us-central1
# VERTEX_AI_PROJECT=${GCP_PROJECT_ID}

# ================================================================
# How to use:
# ================================================================
# 1. For local development:
#    - Copy to .env: cp .env.example .env
#    - Update values in .env
#    - Run: source .env && ./deploy.sh local
#
# 2. For deployment:
#    - Set variables: export GCP_PROJECT_ID=your-project-id
#    - Run: ./deploy.sh deploy
#
# 3. For different environments:
#    - Create .env.production, .env.staging, etc.
#    - Source before deploying: source .env.production && ./deploy.sh deploy
# ================================================================